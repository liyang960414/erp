# ERP 服务配置示例
# 复制此文件为 config.env 并修改配置值
# 使用方式: .\build-with-config.ps1 -ConfigFile config.env

# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_NAME=erp_db
DB_USERNAME=postgres
DB_PASSWORD=your_password_here

# 服务器配置
SERVER_PORT=8080

# JWT 配置（可选）
JWT_SECRET=mySecretKeyForJWTTokenGenerationAndValidationMustBeAtLeast256Bits
JWT_EXPIRATION=86400000

# 日志配置（可选）
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=DEBUG
LOG_FILE_NAME=./logs/erp.log

# 连接池配置（可选）
DB_POOL_MIN_IDLE=10
DB_POOL_MAX_SIZE=20
DB_POOL_MAX_LIFETIME=1200000
DB_POOL_IDLE_TIMEOUT=600000
DB_POOL_CONNECTION_TIMEOUT=30000

